import 'package:flutter/material.dart';

class DegerlendirmelerimiListelePage extends StatelessWidget {
  const DegerlendirmelerimiListelePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Değerlendirmelerimi Listele'),
      ),
      body: Center(
        child: const Text('Değerlendirmelerimi Listele Sayfası'),
      ),
    );
  }
}
