import 'package:flutter/material.dart';

class HasatlarimiGosterPage extends StatelessWidget {
  const HasatlarimiGosterPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Hasatlarımı Göster'),
      ),
      body: Center(
        child: const Text('Hasatlarımı Göster Sayfası'),
      ),
    );
  }
}
